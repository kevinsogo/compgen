#include <cstdio>
using namespace std;
typedef long long ll;

int main() {
    ll z, n, s, v;
    for (scanf("%lld", &z); z--; printf("%lld\n", s)) for (s = 0, scanf("%lld", &n); n--; scanf("%lld", &v), s += v);
}